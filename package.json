{
  "name": "nodejs-docker-jenkins-app",
  "version": "1.0.0",
  "description": "Simple Node.js app with Docker and Jenkins pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
